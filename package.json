{
  "name": "test-orders",
  "version": "1.0.0",
  "description": "Square Test Orders App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-all": "npm install && cd src && npm install"
  },
  "dependencies": {
    "axios": "^1.6.5",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
